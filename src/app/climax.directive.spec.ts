import { ClimaxDirective } from './climax.directive';

describe('ClimaxDirective', () => {
  it('should create an instance', () => {
    const directive = new ClimaxDirective();
    expect(directive).toBeTruthy();
  });
});
